{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "apps/web-ele/dist",
        "buildCommand": "pnpm install && pnpm run build:ele"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/web-ele/dist/$1"
    }
  ]
}
